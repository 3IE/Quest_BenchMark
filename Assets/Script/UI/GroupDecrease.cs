using UnityEngine;

namespace Script.UI
{
    public class GroupDecrease : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
